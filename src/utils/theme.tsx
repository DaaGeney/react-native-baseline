// @packages
import { MD3LightTheme as DefaultTheme } from 'react-native-paper';

const colors = {
  juneBud: '#BADA55',
};

const theme = {
  ...DefaultTheme,
  colors: {
    primary: colors.juneBud,
  },
};

export default theme;